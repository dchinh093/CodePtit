#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    for (int i = 1; i <= t; i++)
    {
        int n;
        scanf("%d", &n);
        int k = 1;
        printf("%.15f\n", (double)1 / n);

    }
    return 0;
    
}